- recordar usar EtM (encrypt then mac)
- RSA: permite a alicia entregar la clave publica (n,e) para que cualquiera encripte mensajes y se los envie (solo ella puede desencriptar).
- Firmas digitales: alicia distribuye una clave publica a sus verificadores. 
  Luego, alicia genera una firma para un documento M usando su clave privada. 
  Luego cualquier persona (verificador) puede verificar que la firma que se le pase es efectivamente de alicia para el documento M
  Certificados
- autenticacion de textos encriptados con clave publica: si no se autentica, 
un adversario puede inyectar claves que   podrian ser usadas por el usuario para encriptar sus archivos

- cada archivo tendrá una id
- cada archivo podrá desencriptarse con una llave
- hacer que el usuarios guarde dichas llaves localmente (el servidor nunca las ve)

ok thanks, first i want to try something... So ive been playing around with the rsa library and 
i was thinking that a simple way of having this encrypted cloud for sharing files would be to do something like 
attach to each user a list of dictionaries containing username:[list(public_key)] where each public_key

DSA: digital signature algorithm. 
ECDSA: eliptic curve digital signature algorithm

esquema hibrido, archivos se encriptan simetricamente
cosas que sabe el servidor adversario:
- Sabe quienes pertenecen a cuales grupos y conoce sus claves publicas
- podria crear grupos no deseados y añadir ahí a usuarios, 
  debido a que la creacion de grupos no está autenticada...


- un archivo pertenece a un solo grupo. podria hacerce que pertenezca a varios grupos?